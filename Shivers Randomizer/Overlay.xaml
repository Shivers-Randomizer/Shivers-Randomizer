<Window x:Class="Shivers_Randomizer.Overlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Shivers_Randomizer"
        mc:Ignorable="d"
        Title="Overlay"
        SizeToContent="WidthAndHeight"
        AllowsTransparency="true"
        WindowStyle="None"
        Background="Transparent"
        Topmost="False"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Canvas Width="1920" Height="1080" >
        <!-- Existing Label -->
        <Label Name="labelOverlay" Content="Test Overlay Text" Canvas.Left="10" Canvas.Top="10" Foreground="Lime"/>

        <Grid Name="overlayGrid" Canvas.Left="120" Canvas.Top="90">
            <!-- White Square -->
            <Rectangle Name="overlayRectangle" Width="320" Height="250" Fill="White" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

            <!-- Text Inside the White Square (Centered) -->
            <TextBlock Name="overlayText" Text="GEOFFREY" Foreground="Black" FontSize="14"
               HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
    </Canvas>
</Window>
