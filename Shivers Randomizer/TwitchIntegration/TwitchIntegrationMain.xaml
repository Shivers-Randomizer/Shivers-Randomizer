<Window x:Class="Shivers_Randomizer.TwitchIntegrationMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:Shivers_Randomizer"
        xmlns:properties="clr-namespace:Shivers_Randomizer.Properties"
        mc:Ignorable="d" 
        Height="600" Width="1020" Background="#FF333333" MinHeight="577" MinWidth="1020"
        Title="Twitch Integration"
        DataContext="{x:Static properties:Settings.Default}">

    <Window.Resources>
        <Style x:Key="labelEnabled">
            <Style.Triggers>
                <Trigger Property="Label.IsEnabled" Value="True">
                    <Setter Property="Label.Foreground" Value="White" />
                </Trigger>
                <Trigger Property="Label.IsEnabled" Value="False">
                    <Setter Property="Label.Foreground" Value="Gray" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Main grid with two columns -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <!-- Left: existing chat and controls -->
            <ColumnDefinition Width="250" />
            <!-- Right: user list + buttons -->
        </Grid.ColumnDefinitions>

        <!-- LEFT SIDE: existing controls -->
        <Grid Grid.Column="0">
            <Button x:Name="buttonConnect" Content="Connect" FontSize="16" Foreground="White" Background="#FF585858" Height="24" Width="92" HorizontalAlignment="Left" VerticalAlignment="Top" IsDefault="True" Padding="0,0" Margin="10,10,0,0" Click="buttonConnect_Click"/>
            <Label Content="Twitch Channel:" FontSize="16" Foreground="White" Width="120" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="110,6,0,0"/>
            <TextBox x:Name="txtBoxTwitchChannel" Text="GodlFire" FontSize="16" Background="#FFF0F0F0" Height="24" Width="150" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="235,10,0,0"/>
            <Label Content="OAuth Token:" FontSize="16" Foreground="White" Width="104" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="390,6,0,0"/>
            <TextBox x:Name="txtBoxToken" FontSize="16" Background="#FFF0F0F0" Height="24" Width="150" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="499,10,0,0"/>

            <RichTextBox x:Name="rtbReceivedMessages" Background="#FF333333" FontSize="16" Foreground="White" IsReadOnly="True" Margin="0,50,0,0" Block.LineHeight="1" VerticalScrollBarVisibility="Visible" MinWidth="21" IsUndoEnabled="False" UndoLimit="0"/>
        </Grid>


        <StackPanel Grid.Column="1" Margin="10" VerticalAlignment="Top">
            <Label Content="Current Players" FontSize="16" Foreground="White" Margin="0,0,0,5"/>
            <ListView x:Name="listViewUsers" Height="377" Background="#FF444444" Foreground="White">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Player" DisplayMemberBinding="{Binding Name}" Width="120"/>
                        <GridViewColumn Header="Points" DisplayMemberBinding="{Binding Points}" Width="80"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <Button x:Name="buttonStartGame" Content="Start Game" FontSize="16" Foreground="White" Background="#FF585858" Height="32" Width="200" Margin="0,10,0,0" Click="buttonStartGame_Click"/>
            <Button x:Name="buttonResetGame" Content="Reset Game" FontSize="16" Foreground="White" Background="#FF585858" Height="32" Width="200" Margin="0,10,0,0" Click="buttonResetGame_Click"/>
            <Button x:Name="buttonSettings" Content="Settings" FontSize="16" Foreground="White" Background="#FF585858" Height="32" Width="200" Margin="0,10,0,0" Click="buttonSettings_Click"/>
        </StackPanel>
    </Grid>
</Window>